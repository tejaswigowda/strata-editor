// Single source of truth for the Strata version string. Bump manually per
// release. The /about/callcard end-card stamps this on every live view and
// every render, so a baked short is self-documenting about which version
// produced it (see guides/GIT_VERSIONING.md-style provenance thinking).
export const STRATA_VERSION = '0.1.0';
